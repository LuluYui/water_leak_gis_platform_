# OpenCode System Instructions

## UI Verification

Always verify UI-related implementations and changes rigorously using the browser automation tools (`browser_open_tab`, `browser_screenshot`, `browser_console`, etc.). Do not assume UI changes work without visual or functional confirmation in the browser when applicable.
